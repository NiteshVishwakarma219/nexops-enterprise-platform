# In local dev, leave this unset — vite.config.js proxies /api to localhost:8000.
# In production (Vercel), set this to your deployed Render backend URL + /api, e.g.:
VITE_API_URL=https://your-app-name.onrender.com/api
